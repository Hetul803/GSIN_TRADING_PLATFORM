
# MCN – Memory Compression Networks (Prototype)

Local dev package for a Memory Compression Network (MCN) layer: add embeddings, score/decay,
compress similar vectors (value-weighted centroids), and retrieve by similarity × value.
